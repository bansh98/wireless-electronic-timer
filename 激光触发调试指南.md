# 激光触发调试指南

## 🎯 当前状态

✅ **已解决:** NRF通信正常,能接收"GO"信号
❌ **待解决:** 激光触发不工作,无法停止计时

---

## 🔧 刚才添加的调试功能

### 功能1: 实时显示激光状态

当系统进入RUNNING状态后,OLED会显示:
```
RUNNING!!!
T:X.Xs L:Y
```
- `T:X.Xs` = 已运行时间
- `L:Y` = 激光状态 (1=有光, 0=遮挡)

### 功能2: 捕获触发检测

如果激光触发了但race_state不是1,会显示:
```
Laser triggered
but state=X
```

---

## 📋 测试步骤

### 第1步: 重新编译烧录
```
1. 编译 Finish_Node
2. 烧录到STM32
3. 上电测试
```

### 第2步: 触发"GO"信号

按Start节点的按键,Finish应该显示:
```
RUNNING!!!
T:0.0s L:1  ← 激光状态
```

### 第3步: 观察激光状态值

**正常情况:**
- 没遮挡时: `L:1` (高电平,有光)
- 遮挡时: `L:0` (低电平,无光)

**或者反过来:**
- 没遮挡时: `L:0`
- 遮挡时: `L:1`

**如果L值一直不变** → 激光传感器没连接或坏了

---

## 🔍 根据测试结果判断

### 情况A: L值会变化 (0↔1)

**说明:** 激光传感器工作正常,但触发极性可能反了

**解决方案:** 修改触发沿

如果遮挡时L=1(上升沿),需要改tim.c:

```c
// 从下降沿改成上升沿
sConfigIC.ICPolarity = TIM_INPUTCHANNELPOLARITY_RISING;
```

### 情况B: L值一直是1或一直是0

**说明:** 激光传感器没连接或信号线有问题

**检查:**
1. 激光接收器VCC/GND是否有电
2. 信号线是否连接到PA0
3. 用万用表测PA0电压变化

### 情况C: 显示"Laser triggered but state=X"

**说明:** 中断触发了,但race_state不对

**可能原因:**
- race_state在某个地方被改了
- 或者触发太快/太慢

---

## 🔌 硬件连接检查

### 激光接收器 → STM32

```
VCC  → 5V 或 3.3V
GND  → GND
OUT/DO → PA0 (TIM2_CH1)
```

### 激光传感器类型

**类型1: 光电对射式**
- 发射端和接收端分开
- 平时有光: OUT=高电平
- 遮挡时: OUT=低电平
- 触发极性: **下降沿** (当前配置)

**类型2: 反射式**
- 发射接收一体
- 平时无反射: OUT=高电平
- 有物体: OUT=低电平
- 触发极性: **下降沿** (当前配置)

**类型3: 其他类型**
- 可能极性相反
- 需要改成上升沿触发

---

## ⚡ 快速测试方法

### 方法1: 用手指测试PA0

不接激光传感器,直接用手指碰PA0:
1. 手指碰PA0 → 应该触发(因为人体有电容)
2. 如果能触发 → 说明代码正常,是激光传感器问题
3. 如果不能触发 → 说明中断配置有问题

### 方法2: 用导线测试

1. 运行状态下,用导线把PA0短接到GND
2. 观察是否触发
3. 如果触发 → 说明需要下降沿
4. 如果不触发,试试短接到3.3V

---

## 🛠️ 可能需要的修改

### 修改1: 改触发沿 (如果极性反了)

打开 `Finish_Node/Core/Src/tim.c` 第59行:

```c
// 原来(下降沿):
sConfigIC.ICPolarity = TIM_INPUTCHANNELPOLARITY_FALLING;

// 改成(上升沿):
sConfigIC.ICPolarity = TIM_INPUTCHANNELPOLARITY_RISING;

// 或者(双沿):
sConfigIC.ICPolarity = TIM_INPUTCHANNELPOLARITY_BOTHEDGE;
```

### 修改2: 添加滤波 (如果信号不稳定)

tim.c第62行已有滤波器:
```c
sConfigIC.ICFilter = 8; // 已经有了
```

如果还不够稳定,可以改成15(最大值)

---

## 📞 测试完告诉我

1. ✅ RUNNING时,L值是多少? (1还是0)
2. ✅ 遮挡激光时,L值会变化吗? (变成什么)
3. ✅ 遮挡后有没有触发? (显示什么)
4. ✅ 激光传感器接线确认: OUT → PA0 ?

有了这些信息我就能精确定位问题!

