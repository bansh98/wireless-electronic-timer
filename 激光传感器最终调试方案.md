# 激光传感器最终调试方案

## 🎯 当前状态

### ✅ 已确认工作
- MP3播放正常 - 3个音频都能播放
- NRF通信正常 - 能接收GO信号

### ❌ 待解决
- 激光传感器不触发停止计时

---

## 🔧 刚才的修改

### 修改1: 强制中断调试输出

现在TIM2输入捕获中断触发时,**无论什么状态**都会立即显示:
```
INT Triggered!
State: X
```

### 修改2: 运行状态显示

RUNNING状态时每秒更新:
```
RUNNING!!!
Time: Xs
Wait laser...
```

---

## 🧪 测试步骤

### 步骤1: 重新编译烧录Finish_Node

### 步骤2: 触发GO信号

按Start按键,Finish应该显示:
```
RUNNING!!!
Time: 1s
Wait laser...  ← 每秒更新
```

### 步骤3: 遮挡激光传感器

**情况A: 如果显示"INT Triggered! State: 1"**
✅ 说明中断能触发!问题已解决!
→ 应该会接着显示FINISH和时间

**情况B: 如果显示"INT Triggered! State: 0"**
❌ 中断触发了,但是状态不对
→ 说明在GO信号之前就触发了
→ 可能需要屏蔽掉初始的触发

**情况C: 如果什么都不显示**
❌ 中断根本没触发
→ PA0配置或激光传感器连接有问题

---

## 🔍 如果中断不触发(情况C)

### 检查1: PA0引脚连接

用万用表测量:
```
激光接收器OUT ←→ STM32 PA0
应该导通(蜂鸣器响)
```

### 检查2: 激光传感器输出

用万用表测量激光接收器OUT引脚电压:
```
有光时: ___V (平时)
遮光时: ___V (触发)
```

**正常应该:**
- 有光时: 3.3V (高电平)
- 遮光时: 0V (低电平) ← 下降沿触发

**或者:**
- 有光时: 0V
- 遮光时: 3.3V ← 需要改成上升沿

### 检查3: 手动触发测试

**用导线强制触发:**

1. **运行状态时,PA0短接到GND**
   - 模拟下降沿
   - 应该显示"INT Triggered!"

2. **如果不触发,试试PA0接3.3V**
   - 然后拔掉(模拟上升沿后下降沿)

---

## 🔧 如果是极性问题

### 修改触发沿

打开 `Finish_Node/Core/Src/tim.c` 第59行:

**当前(下降沿):**
```c
sConfigIC.ICPolarity = TIM_INPUTCHANNELPOLARITY_FALLING;
```

**如果需要上升沿:**
```c
sConfigIC.ICPolarity = TIM_INPUTCHANNELPOLARITY_RISING;
```

**或者双沿(任意变化都触发):**
```c
sConfigIC.ICPolarity = TIM_INPUTCHANNELPOLARITY_BOTHEDGE;
```

---

## 💡 可能的问题

### 问题1: PA0被其他功能占用

检查`gpio.c`中是否有PA0的配置。
PA0应该只在`tim.c`的HAL_TIM_IC_MspInit中配置。

### 问题2: TIM2中断优先级太低

检查`stm32f1xx_it.c`的TIM2_IRQHandler是否存在。

### 问题3: 激光传感器需要上拉

某些激光传感器是开漏输出,需要上拉电阻。

在tim.c的HAL_TIM_IC_MspInit中修改:
```c
GPIO_InitStruct.Pull = GPIO_PULLUP; // 添加上拉
```

---

## 📞 测试后告诉我

1. ✅ RUNNING时能看到"Time: Xs"吗? ____
2. ✅ 遮挡激光后显示什么? ____
   - [ ] INT Triggered! State: 1
   - [ ] INT Triggered! State: 0  
   - [ ] 什么都没显示
3. ✅ 激光OUT电压: 有光___V, 遮光___V
4. ✅ PA0短接GND能触发吗? ____

根据这些信息能100%定位问题!

